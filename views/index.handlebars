<h1>BucketList!</h1>

<h2>BucketList  that are not complete!</h2>

<ul>
  {{#each bucketItem}}
    {{#unless complete}}
      {{> bucketlist-block complete=true}}
      {{!-- This carrot means partials are in use --}}
    {{/unless}}
  {{/each}}
</ul>

<h2>BucketList Items that are complete!</h2>

<ul>
  {{#each bucketItem}}
    {{#if complete}}
      {{> bucketlist-block complete=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a BucketList Item</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">BucketList Item Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="complete">Complete?</label><br>
    <input type="radio" name="complete" value="1" checked> Complete<br>
    <input type="radio" name="complete" value="0"> Todo
  </div>

  <button type="submit">Add BucketList Item</button>
</form>
